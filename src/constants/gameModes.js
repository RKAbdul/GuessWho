// Mode indices shared between the home screen (mode selection/config) and
// each room, so game modes aren't referenced as unlabeled 0/1/2 magic numbers.
export const GAME_MODES = {
    IMPOSTER_WORD: 0,
    ANSWER_THE_QUESTION: 1,
    WHO_ANSWERED: 2,
    DESCRIBE_GUESS: 3,
};
